# Server
PORT=3000

# Email (configure with your real SMTP credentials)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=hello@maccollab.ro
SMTP_PASS=your_app_password_here
EMAIL_TO=hello@maccollab.ro

# Set to "true" to actually send emails, "false" to just log them
SEND_EMAIL=false
